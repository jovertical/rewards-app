{
  "name": "root",
  "private": true,
  "scripts": {
    "setup": "./node_modules/.bin/lerna clean -y && ./node_modules/.bin/lerna bootstrap",
    "dev": "./node_modules/.bin/lerna run dev --parallel",
    "build": "./node_modules/.bin/lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engineStrict": true,
  "engines": {
    "node": ">= 14.x"
  }
}
